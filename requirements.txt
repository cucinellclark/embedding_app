# Core dependencies for TF-IDF embedding processing
numpy>=1.20.0
scikit-learn>=1.0.0
nltk>=3.6.0

# HuggingFace datasets and PyArrow for efficient data storage
datasets>=2.0.0
pyarrow>=10.0.0

# Text processing and utilities
requests>=2.25.0

# Development and testing (optional)
pytest>=6.0.0
black>=22.0.0 